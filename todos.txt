In progress
- Share 50 trips to socials

P0

P1 
- Animation mode (retry with a different approach)
- Filter data by date range before injestion
- Bug: Slide title disappears in form (to be replicated)

P2
- Move watermark a bit to the left to not overlap with IG slideshow
- Improve layout for mobile
- Remove settings step in wizard
- Option to resize slide window (within certain limits) to help better fit the map
- Change marker look, marker size
- Go to previous/next trip
- Support for photos (many subfeatures)

P??
- Use places API to get info about each marker in the trip and use that to improve tagging
- When the points in a map are too sparse, use routing API
- Do something about overlapping markers?
- More layout options: Resize slide, left/center/right align element
- Download & upload data model (to migrate to different machine) --> Hosted backend
